import { UserPortalLayout } from './UserPortalLayout'

// Note: DocumentViewerPage handles its own internal two-panel layout.
// This layout provides the top nav shell; the page itself renders the sidebar.
export function DocumentViewerLayout() {
  return <UserPortalLayout />
}
